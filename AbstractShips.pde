/*

What are abstract classes and why do we use them?

Why does it make sense to make Ship abstract?

*/

Ship ds;
Ship sd;

void setup() {
  size(500, 500);

  ds = new DeathStar();
  sd = new StarDestroyer();
}

void draw() {
  background(0);

  // call methods
}
